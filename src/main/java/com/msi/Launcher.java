package com.msi;

import com.msi.testlinkFront.Application;

// the Launcher class need for executable jar file. The class which extends another does not suite (Application)
public class Launcher {
    public static void main(String[] args) {
        Application.main(args);
    }
}
